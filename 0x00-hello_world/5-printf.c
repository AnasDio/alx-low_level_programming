#include <stdio.h>
/**
 * main - a program to output a text using printf
 *
 * Return:0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
