#include "main.h"
/**
 * print_last_digit - as the name says
 *
 * @a: the integer to be used
 *
 * Return: last digit
 */
int print_last_digit(int a)
{
int b = a % 10;
_putchar('b');
return (a % 10);
}
